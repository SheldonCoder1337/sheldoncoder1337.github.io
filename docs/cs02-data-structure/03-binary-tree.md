# 二叉树

## 快速

=== "C"
    ```c
    void traverse(vector<int>& arr) {
        for (int i = 0; i < arr.size(); i++) {
            // 迭代访问 arr[i]
        }
    }
    ```

=== "Python"
    ```python
    def traverse(arr: list[int]):
        for i in range(len(arr)):
            # 迭代访问 arr[i]
            pass
    ```

=== "rust"
    ```rust
    def traverse(arr: list[int]):
        for i in range(len(arr)):
            # 迭代访问 arr[i]
            pass
    ```
